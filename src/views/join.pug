extends base

block content 
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(placeholder="Name" name="username", type='text',requried)
        input(placeholder="Email" name="email", type='email',requried)
        input(placeholder="Username" name="name", type='text',requried)
        input(placeholder="Password" name="password", type='password',requried)
        input(placeholder="Confirm Password" name="password2", type='password',requried)
        input(placeholder="Location" name="location", type='text')
        input(type='submit',value='Join')
    hr
    div 
        span Already have an account? 
        a(href="/login") Log in now &rarr;